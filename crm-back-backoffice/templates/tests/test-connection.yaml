apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "crm-back-backoffice.fullname" . }}-test-connection"
  labels:
    {{- include "crm-back-backoffice.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "crm-back-backoffice.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
