apiVersion: v1
kind: Service
metadata:
  name: {{ include "crm-back-backoffice.fullname" . }}
  labels:
    {{- include "crm-back-backoffice.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "crm-back-backoffice.selectorLabels" . | nindent 4 }}
