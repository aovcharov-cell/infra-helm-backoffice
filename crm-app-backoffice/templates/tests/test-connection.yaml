apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "crm-app-backoffice.fullname" . }}-test-connection"
  labels:
    {{- include "crm-app-backoffice.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "crm-app-backoffice.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
